include(luajit.cmake)
add_subdirectory(luabind)
add_subdirectory(cximage)
add_subdirectory(GameSpy)
add_subdirectory(OPCODE)
add_subdirectory(ode)
if (MEMORY_ALLOCATOR STREQUAL "mimalloc")
    set(MI_BUILD_TESTS     OFF CACHE BOOL "Override for mimalloc option MI_BUILD_TESTS")
    set(MI_BUILD_DYNAMIC   OFF CACHE BOOL "Override for mimalloc option MI_BUILD_DYNAMIC")
    set(MI_NO_NEW_OVERRIDE ON  CACHE BOOL "Override for mimalloc option MI_NO_NEW_OVERRIDE")
    add_subdirectory(mimalloc)
    set_property(TARGET mimalloc-static PROPERTY POSITION_INDEPENDENT_CODE ON)
endif()
