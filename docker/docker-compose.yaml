version: "3"

services:
  xray:
    build:
      context: image
      dockerfile: Dockerfile
    container_name: xray-image
    cap_add:
      - SYS_PTRACE
    stdin_open: true
    tty: true
    volumes:
      - <mount_path>:/xray-16
    ports:
      - "127.0.0.1:2224:22"
